(module (
  class_definition
    name: (identifier) @definition.class.name
    superclasses: (
      argument_list (
        (identifier)
      )
    )* @definition.class.superclasses
  ) @definition.class
)